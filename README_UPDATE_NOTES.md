# README Update Patch

This patch updates the root `README.md` to reflect the current completed state of the project.

Major README changes:

- Marks core search engine workflows as implemented.
- Adds frontend page list.
- Adds MS/MS spectrum detail API route.
- Adds LC-MS/MS cosine search API route and curl example.
- Adds docs index section matching the current `docs/` tree.
- Adds updated current project status.
- Moves CCS, parent ion mass filtering, ML/research APIs, group-by-compound, and sortable LC-MS/MS columns into future/polish work.
- Uses `src/validation/spectra.schemas.ts` in the structure highlights.

Home page note:

If the previous home page patch was applied, no additional home page update is needed. The README now matches the current home page direction with `/search/ms-ms` included as a primary search workflow.
